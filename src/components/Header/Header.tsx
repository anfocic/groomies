import './Header.css';

const Header = () => {
    return (
        <header className="header">
            <a href="/" className="header-logo">
                <img src="title.webp" alt="Logo"/>
            </a>
        </header>
    );
};

export default Header;