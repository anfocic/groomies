import './Hero.css';

const Hero = () => {
    return (
        <section className="hero">
            <div className="hero-content">
                <h1 className="hero-title">Groomies</h1>
                <p className="hero-subtitle">Empowering you with innovative solutions to drive success.</p>
                <button className="hero-button">Book now</button>
            </div>
        </section>
    );
};

export default Hero;